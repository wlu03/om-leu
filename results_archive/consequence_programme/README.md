# Consequence-modelling programme (E1–E7): archived summaries

Copied from the artifact root `structure_descent-consequence-20260910T094713Z-31092-artifacts/`,
which sits beside the repository. Only the per-run `result.json` summaries, the status ledger,
the generated reports and the E1 audit and generation manifests are kept here. Per-event
predictions (about 8 GB) remain in the artifact root; `paper/make_tables.py` reads the metrics
from these summaries, and the paired intervals need the artifact root.

Runs under `superseded_*` directories in the artifact root were produced before two fixed
defects and are excluded. The code is on the branches `exp/consequence-20260910T094713Z-31092/*`,
with everything merged on `.../90-integration`.
