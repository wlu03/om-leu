.env
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.ipynb_checkpoints/
*.ipynb_checkpoints
.DS_Store
dist/
build/
*.egg-info/
**.pdf
**/colab_runner.ipynb
**/NEXT_STEPS.md
paper/

# Documentation (kept local)
docs/

# Per-dataset directories: <name>/data (raw + prepared), <name>/results
# (run outputs), <name>/cache (outcomes / embeddings sqlite). Only the
# Amazon field description is versioned.
*/data/*
*/results/*
*/cache/*
!amazon/data/fields.csv

# Tooling state
.pytest_cache/
.mypy_cache/
.ruff_cache/
tmp/
.claude/
runs/
experiments/data/

# ablation/: per-run JSON (per-event NLL lists) stay local; results.md + README.md are tracked
ablation/*/results/*.json

# experiment package byte-code
omleu_experiments/**/__pycache__/
